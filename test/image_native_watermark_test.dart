import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  test('ImageNativeWatermark MethodChannel exists', () {
    expect(true, isTrue);
  });
}
